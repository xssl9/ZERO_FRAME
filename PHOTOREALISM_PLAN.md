# ZERO FRAME — план фотореализма

## Статус на 1 сентября 2026

- [x] Все прежние карты удалены вместе с `LevelBuilder`; единственный уровень — статический `scenes/levels/dev_test_grid.tscn` (greybox-грид для тестов, дневное HDRI-небо).
- [x] `WeaponTuningCamera` каждого оружия является фактической current-камерой оружейного `SubViewport`.
- [x] Единый HDR panorama sky, AgX, фиксированная экспозиция, мягкий bloom и нейтральный contrast.
- [x] Performance/High/Ultra переключаются в главном меню; недоступные SSR/SSIL/probes не включаются в Compatibility.
- [x] Poly Haven `grass_medium_01` использует low-poly tuft + MultiMesh, без коллизии и без player-interaction physics.
- [x] Перезапустить CLI smoke-test на новой карте (Player на полу, AK camera current, parser/runtime errors нет).
- [x] Звук: выстрелы, шаги и удары пуль слышны; оружейные голоса непозиционные, потому что в оружейном `SubViewport` нет 3D-листенера.
- [x] AK-74M: селектор АВТО/ОД на клавише V, тяжёлая отдача уводит реальный прицел, а не только картинку.
- [x] Затвор совпадает с выстрелами: клип выстрела сжимается до одного полного цикла на патрон (`speed_scale = длина_клипа / интервал`) и перезапускается `seek` на каждом выстреле — 30 циклов на 30 патронов.
- [x] Отдача уровня Rust: несжатый магазин AK уводит прицел примерно на 49° вверх и 8° вправо, отдача растёт от нагрева ствола, пистолет отрабатывает щелчком и восстанавливается между выстрелами.
- [x] Видимая вспышка выстрела: процедурный `shaders/muzzle_flash.gdshader` (premultiplied alpha, потому что оружейный `SubViewport` прозрачный), рваные языки пламени на fbm-шуме с градиентом по температуре, три лепестка через 60° плюс фронтальная «звезда», ядро, гало, искры и пороховой дым, разброс размера и яркости на каждый патрон. Вспышка живёт 42 мс, свет — 75 мс.
- [x] Вспышка освещает уровень: второй `OmniLight3D` ставится в мир уровня по трансформу дула, снятому относительно оружейной камеры. Свет внутри оружейного `SubViewport` до стен не достаёт физически — там свой `World3D`.
- [x] Видимые лучи света: объёмный туман с прямым рассеиванием (anisotropy 0.42), `light_volumetric_fog_energy` на солнце, фонарике и вспышке, объём 128³, перекрытие из девяти балок над центром арены как то, что режет солнце. Плотность — один экспорт `shaft_density`.
- [x] Солнце против ambient: было `light_energy = 2.2` против `ambient_light_energy = 0.72` от HDRI, из-за чего сцена освещалась почти целиком небом и теней на полу не было вообще. Теперь 5.0 против 0.38.
- [x] Небо стало настоящим HDR: `mud_road_puresky_2k.hdr` в проекте имеет пик яркости 0.8 при медиане 0.31 — диапазона нет, солнечного диска нет. Заменено на `kloofendal_43d_clear_puresky_2k.hdr` (пик около 110 000, диск на восьми пикселях), скачано через `tools/fetch_polyhaven.py`.
- [x] Солнце и небо смотрят в одну сторону: трансформ `PhysicalSun` вычислен из панорамы `scripts/tools/align_sun_to_sky.gd` (42.9° над горизонтом, азимут 216.2°), `sky_rotation` обнулён, иначе привязка ломается.
- [x] Симуляция света на HIGH: SDFGI (конус-трейс по SDF сцены, единственное, что в Godot вообще похоже на трассировку), SSIL, SSR, SSAO, объёмный туман с `gi_inject = 1.0`, то есть в тумане рассеивается и отражённый свет. ULTRA: шесть каскадов SDFGI, SSR на 64 шага, TAA. Аппаратной трассировки в Godot 4.7 нет вообще.
- [x] Погода выбирается в меню: кнопка `ПОГОДА` переключает ЯСНО/ДОЖДЬ, уровень читает `zero_frame/weather` при загрузке. Ясно — светлее, теплее, воздух тоньше, солнце 3.1; дождь — темнее, холоднее, тумана вдвое больше, солнце 2.4 но втрое сильнее светит в воздух. Оба грейда заданы абсолютными значениями, поэтому переключение туда-обратно не уводит картинку.
- [x] Дождь и слякоть: `scripts/environment/rain_system.gd` (объём капель 30 × 14 × 30 м, едет за игроком, плюс брызги у ног) и полноэкранный проход `shaders/rain_puddles.gdshader` от пользователя (CC0, shadecore_dev): лужи собираются по двум октавам шума и нормали из буфера глубины, по ним идут круги от капель, отражения ray-march по экрану. Мокрость greybox-материалов — один uniform `wetness` в `dev_grid.gdshader`: темнее альбедо и вдвое ниже roughness.
- [x] Дождь слышно: бесшовная петля 28.5 с (кроссфейд хвоста на начало) на непозиционном плеере, громкость идёт от `RainSystem.intensity`, в ясную погоду не запускается вообще.
- [x] Перезарядка АК звучит: 4.24 с записи (магазин вон, магазин в, затвор), длительность перезарядки берётся из длины записи, а `Rig|AK_Reload_full` растягивается под неё через `speed_scale = 0.825`.
- [x] Альбедо greybox приведено к бетону (0.33/0.26 вместо 0.60/0.45): в белой коробке с альбедо 0.6 GI засветляет все тени и в кадре не остаётся ни одной тени.
- [x] Звук: пользовательские записи из `game/sounds` перемастерены в проект. Автомат громкий (компрессия и лимитер, а не только пик), пистолет пересемплирован вниз на 20 %, шесть отдельных шагов бега вместо одного сэмпла, пять вариантов отдышки на десятой секунде спринта на уровне около -23 dB. Шина `Weapons` с хард-лимитером -1 dBFS.
- [x] Камеры оружия редактируются вручную: `WeaponTuningCamera` стоит в начале координат сцены оружия с единичным трансформом, `fov`/`keep_aspect`/`near`/`far` берутся из сцены, `MuzzlePoint` — перетаскиваемая точка вспышки.
- [x] Вторая карта `PVP — LINSE`: скан из RealityCapture, коллизия trimesh на 48k треугольников строится при загрузке. Пол скана был наклонён на 2.22° — выровнен по площадно-взвешенной подгонке плоскости. Единицы скана не метры (этаж 43 юнита), масштаб 0.075 даёт этаж 3.2 м и оболочку 34 × 7 м.
- [x] Скан освещается как скан: `lighting_model = SCAN_FLAT` — равномерный белый ambient, солнце ослаблено до 0.45 и без теней, SSAO/SSIL/SDFGI/туман выключены. Иначе всё, куда не попадал прямой свет, было чёрным: у фотограмметрии тени и AO уже в альбедо.
- [x] Свет и погода дошли до рук и оружия: оружейный `SubViewport` получает ambient, экспозицию и направление солнца из уровня (в камерном пространстве, поэтому блик едет при повороте), а в дождь внутри вьюпорта падают капли.
- [x] Пол и стены тестовой карты на Poly Haven PBR: `worn_tile_floor` и `concrete_block_wall_03`, мировой триплanar в `dev_grid.gdshader`, ARM-упаковка, нормали через whiteout-блендинг трёх проекций.
- [x] В главном меню крутится панорама: сцена PvP-карты рендерится в `SubViewport` за кнопками, камера стоит в середине, дрейфует 3.2°/с и хватается ЛКМ.
- [ ] Художественные карты: сделать заново с нуля, когда тестовый грид перестанет быть достаточным.
- [ ] Проверить финальный вид на GPU: кадры со «звездой» вспышки и усиленным солнцем ещё не сняты.

## 1. Стабильная база

- Зафиксировать два пресета: 60 FPS для iGPU и High для дискретной GPU.
- Проверять frame time, draw calls, VRAM и shader compilation на каждой карте.
- Убрать runtime-генерацию: карты, Player, свет и камеры должны быть видны в редакторе.

## 2. Физически правильная сцена

- Единица масштаба — 1 метр; проверить двери, ступени, оружие и высоту камеры.
- Отделить render mesh от упрощённой collision mesh; не использовать мелкие детали скана для физики.
- Добавить LOD и visibility range для травы, мусора и мелкого реквизита.

## 3. PBR-материалы

- Одинаковая texel density; 2K для большинства поверхностей, 4K только для hero-assets.
- Проверить каналы ARM, OpenGL normal, roughness и отсутствие металличности у бетона/грунта.
- Разбить повторы декалями грязи, протечек, трещин и краёв, а не чрезмерным post-process.

## 4. Свет и отражения

- PhysicalSky + DirectionalLight3D должны иметь одно направление солнца; экспозиция не должна дробиться.
- AgX, мягкий bloom только от ярких источников; не давить тени contrast-фильтром.
- Low: sky ambient + SSAO. High: SSIL + SSR + reflection probes. SDFGI включать только после замера GPU frame time.
- Статические reflection probes для помещений; SSR не должен быть единственным источником отражений.

## 5. Камера и viewmodel

- WorldCamera рисует мир; WeaponCamera в отдельном SubViewport всегда рисует руки/оружие поверх мира.
- Ракурс берётся из `WeaponTuningCamera` в сцене каждого оружия; runtime не перезаписывает его позицию, fov, keep_aspect, near и far.
- Bodycam sway от реальной velocity, низкая частота шага, быстрый mouse response; шейдер bodycam остаётся слабым.

## 6. Финальный QA

- Эталонные скриншоты: улица, тёмный интерьер, блик на металле, трава вблизи и вдали, выстрел и декаль.
- Проход каждой карты: спавн на полу, нет телепортов, нет провалов, Esc возвращает в меню.
- Приёмка: нет parser/runtime errors; нет первого shader stutter после кэша; пресет 60 FPS укладывается в 16.7 ms.
